# 设施番茄花期熊蜂授粉轮换与安全结案闭环

## 项目目标

建设一个面向设施番茄种植基地的 Go HTTP 后端，以单个授粉周期为一致性边界，协调温室分区、花序批次、熊蜂蜂群、蜂王代次、设备读数、农药安全事件和复核人员。周期从规则与任务锁定开始，经蜂群入区、分时巡飞、覆盖及生物指标核验、异常停飞、跨区轮换、农药冻结恢复、确定性补授粉，最终由两名不同合格人员独立复核后形成唯一完成凭据。系统采用持久化事务、区间冲突图、可消耗巡飞预算、覆盖格、冻结屏障、操作幂等表和仅追加证据流，支持进程重启后恢复未完成周期、设备待重试调用和未决仲裁。实现规模限定为至少 4 个业务包、至少 20 个生产 Go 文件，生产代码控制在 2000—3000 有效行并以约 2500 行为目标；规模来自调度、事务、固定点运算、冻结传播、恢复代次和测试支撑，不包含填充代码。

## 端到端业务流程

1. 创建并锁定授粉周期：校验规则摘要与蜂群健康证明时效，固定温室及分区、花序批次、蜂群和蜂王代次、称重基线、投放及轮换窗口、观察时点、抽样花序、巡飞预算、环境阈值、农药再入规则和任务代次；在同一事务中建立区间占用、预算账户、覆盖格与锁定证据，状态由待锁定进入待入区。
2. 蜂群入区与巡飞采集：验证当前分区、占用窗口和任务代次后登记入区；按观察时点调用环境探头、蜂箱秤和计数适配器，接收访花、回巢、死亡蜂、咬痕花序及未授粉花序数据，完成固定点派生计算与守恒校验后，原子扣减巡飞预算并填充覆盖格，状态进入或保持巡飞中。
3. 异常停飞与设备重试：设备拒绝、断连、超时或格式错误只追加失败证据并创建带逻辑时间的确定性待重试调用；健康、回巢率、连续失重或环境越界触发停飞，不得用推测值填充覆盖格。重试成功后仍须通过当前窗口、代次及冻结屏障校验。
4. 农药冻结与恢复：农药事件依据施用分区、温室漂移邻接图和安全再入间隔计算受影响分区及窗口，在一个事务中冻结相关占用并将周期置为安全冻结；冻结期间回执仅追加为隔离证据。迟到事件保留原始发生时间和到达时间，撤销旧恢复资格并生成新的恢复代次；蜂群撤回、残留核验和再入时刻全部满足后进入待恢复核验并恢复巡飞。
5. 跨区轮换与补授粉：轮换前检查目标分区相容性、跨区携带风险、冻结状态、窗口冲突及剩余预算，原子释放或结束旧占用并建立新占用。覆盖不足、回巢率异常或连续失重通过稳定排序算法合并为唯一补授粉范围，状态进入补授粉中，重新占用受影响分区和观察时点；旧代次迟到回执不得覆盖当前证据。
6. 周期结案与终局竞争：覆盖全部闭合、预算账目守恒、冻结全部解除、蜂群按要求撤离且两名不同合格人员完成独立复核后进入待结案复核；完成、蜂群撤离和取消请求通过终局比较交换式事务竞争，只有一个请求可写入唯一终局及凭据，终态后所有状态变更请求均被稳定拒绝。

## 核心组件与职责

1. 温室与蜂群规则目录：管理温室、分区、漂移邻接图、分区相容规则、花序批次、蜂群及蜂王代次、健康证明摘要、合格复核人员和版本化规则摘要；锁定后周期仅引用不可变快照。
2. 授粉周期聚合：实现待锁定、待入区、巡飞中、安全冻结、待恢复核验、补授粉中、待结案复核、已完成、蜂群撤离、已取消状态机，维护任务与恢复代次、操作幂等、仅追加证据序号和唯一终局。
3. 时间窗及蜂群占用管理器：维护温室分区—时间窗区间冲突图、蜂群跨分区占用、分区蜂群相容性、跨区携带限制及巡飞预算账户，为锁定、轮换、补授粉和并发扣减提供原子裁定。
4. 巡飞覆盖与生物指标账簿：维护按分区、花序、观察时点组成的覆盖格、设备调用及重试、整数守恒校验、称重与回巢率等固定点计算、异常判定和确定性补授粉范围。
5. 农药冻结恢复及结案仲裁器：传播农药冻结屏障，处理迟到事件、撤回及残留核验、恢复代次冲突、双人复核和完成/撤离/取消终局竞争。
6. Go HTTP API：提供 JSON 接口、稳定错误码、操作号幂等语义和确定性错误排序；组合领域服务与持久化事务，并暴露健康检查及重启恢复入口。

## 领域规则与不变量

1. 所有写操作携带 operation_id、逻辑时间和期望任务代次；同一 operation_id 与规范化内容摘要相同则返回首次结果，不同则返回 OPERATION_CONTENT_CONFLICT。操作结果与业务事务共同提交。
2. 时间窗采用左闭右开区间；同一蜂群的任意重叠占用均冲突，同一分区在重叠窗口内仅可接纳规则快照判定为相容的蜂群。冲突原因按温室、分区、窗口起止、蜂群排序。锁定、轮换、补授粉占用和预算扣减必须在单一事务中完成。
3. 覆盖格键为分区、花序批次、观察时点，锁定的每个键只能由匹配当前窗口、当前任务或恢复代次的有效观察首次填充；完全相同的重复回执幂等，不同内容返回 OBSERVATION_DUPLICATE_CONFLICT。缺失键不能结案。
4. 访花总数必须等于各抽样花序访花数之和；咬痕花序数与未授粉花序数均不得为负、不得超过抽样花序数，且两者之和不得超过抽样花序数；回巢数、外飞数和死亡蜂数遵循锁定的整数边界。任何非法观察只进入拒绝证据，不形成覆盖或预算扣减。
5. 重量、温度、湿度、二氧化碳和各类比率均使用明确量纲的 int64 固定点值。派生值使用先约分再乘除、指定半入远离零舍入规则，并检查负号、除零及每一步溢出；计算失败返回稳定错误码且不得写派生结论、覆盖格或预算流水。
6. 设备失败按照周期、设备类型、分区、观察时点、原调用序号生成唯一重试键；状态仅为待重试、成功或永久失败，最大次数来自锁定规则。重试顺序与逻辑退避时刻可由输入完全推导，失败不得伪造读数。
7. 农药冻结范围包含施用分区及漂移邻接图中规则指定层数的邻区，并与事件发生时刻至再入截止时刻相交的占用窗口取交集。冻结期间观察不得推进覆盖；迟到事件若回溯命中已采集或恢复窗口，必须生成更高恢复代次并使旧恢复凭据失效。
8. 补授粉范围由未覆盖格、回巢率异常格和连续失重格的并集生成，按分区、花序、观察时点稳定排序并合并相邻时点，同一触发证据集合只能存在一个活动补授粉任务。结案要求无活动冻结或补授粉、预算初值等于已扣减加剩余值、所有目标格有效、两名不同且合格的复核人员分别签署当前证据根摘要。

## 数据模型与持久化

1. Greenhouse、Zone、ZoneAdjacency：温室及分区标识、漂移邻接边、分区容量和相容标签，使用版本号支持锁定快照。
2. Colony、QueenGeneration、HealthCertificate：蜂群身份、蜂王代次、健康状态、证明有效期和内容摘要；历史版本不可覆盖。
3. PollinationCycle、CycleSnapshot：周期状态、任务代次、恢复代次、逻辑时间、锁定规则摘要、终局版本及当前证据根摘要。
4. InflorescenceBatch、ObservationPoint、CoverageCell：花序批次、抽样花序数、观察时点和覆盖格状态，记录有效观察证据序号与所属代次。
5. WindowOccupancy、FlightBudgetAccount、BudgetEntry：左闭右开窗口、蜂群及分区占用、用途和代次；预算使用初值、剩余值及仅追加借记流水实现守恒检查。
6. Observation、FixedPointReading、DerivedMetric：访花、外飞、回巢、死亡蜂、咬痕和未授粉计数，整数化设备读数、派生比率、舍入信息及校验结果。
7. PesticideEvent、FreezeBarrier、RecoveryCredential：施用与到达逻辑时间、药剂规则、影响分区、再入截止、冻结窗口、撤回和残留核验、恢复代次及凭据失效原因。
8. DeviceCall、RetryAttempt、EvidenceEvent、OperationRecord、Review、TerminalCredential：设备调用脚本结果和重试状态、全局递增的仅追加证据、幂等请求摘要与响应、双人复核及唯一终局凭据；所有核心表持久化到关系数据库并通过事务和唯一约束保证重启恢复。

## 公开接口

1. POST /v1/cycles 与 POST /v1/cycles/{id}/lock：创建并锁定周期；锁定请求包含完整规则版本、窗口、蜂群、预算、覆盖矩阵及 operation_id。
2. POST /v1/cycles/{id}/admit、/rotate、/withdraw 和 /cancel：处理蜂群入区、跨区轮换、撤回及取消，并返回占用版本、预算余额和状态。
3. POST /v1/cycles/{id}/observations 与 POST /v1/cycles/{id}/device-retries/{retry_key}：提交巡飞观察或执行确定性设备重试；响应区分有效覆盖、隔离证据、幂等重复和拒绝。
4. POST /v1/pesticide-events 与 POST /v1/cycles/{id}/recovery-checks：登记农药事件，执行撤回、残留及再入核验，并返回冻结传播范围和恢复代次。
5. POST /v1/cycles/{id}/supplements 与 POST /v1/cycles/{id}/reviews：创建或推进确定补授粉任务，提交独立复核；服务端拒绝调用方指定不同于算法结果的补授粉范围。
6. POST /v1/cycles/{id}/complete、GET /v1/cycles/{id}、GET /v1/cycles/{id}/evidence 和 GET /healthz：竞争结案、查询聚合快照、按序读取证据和检查服务状态。所有错误使用 {code,message,reasons[]}；reasons 按温室、分区、时间窗、花序、观察点和错误码排序。

## 失败边界

1. 规则摘要或健康证明陈旧时在建立任何占用前失败；数据库事务回滚后不得残留周期快照、覆盖格、预算流水或操作成功记录。
2. 占用冲突、分区不相容、跨区携带风险和预算不足在同一串行化事务内裁定；唯一约束或版本冲突转换为稳定业务错误，可安全使用相同 operation_id 重试。
3. 观察校验与固定点派生计算在预算扣减和覆盖写入之前完成；任何守恒、代次、冻结、除零或溢出错误只追加拒绝证据，不能留下部分业务状态。
4. 设备边界只接受显式成功值或显式失败类别；超时由逻辑时钟和故障脚本驱动，崩溃恢复后从持久化 DeviceCall 状态继续，不重复消费成功结果。
5. 农药事件、冻结屏障、旧恢复凭据失效和新恢复代次在一个事务提交；冻结期间到达的并发观察通过屏障版本失败并保留隔离证据。
6. 完成、撤离和取消共享终局唯一约束及聚合版本；竞争失败者返回 TERMINAL_ALREADY_DECIDED。提交成功后重启仍只能读取同一终局和完成凭据。

## 验收标准

1. 锁定操作完整固化温室分区、花序批次、蜂群及健康证明摘要、蜂王代次、称重基线、投放与轮换窗口、巡飞预算、观察矩阵、抽样花序、环境阈值、农药再入规则和任务代次；陈旧输入整体失败。
2. 并发锁定、轮换和预算扣减原子裁定同蜂群窗口重叠及分区相容性；相同操作号同内容重试返回相同结果，不同内容稳定报冲突，失败无部分状态。
3. 巡飞回执必须匹配当前分区、窗口和代次，并通过访花、回巢、死亡蜂、咬痕及未授粉整数守恒；只有合法且非重复数据填充完整覆盖矩阵。
4. 所有重量、回巢率、咬痕覆盖率、温度、湿度和二氧化碳计算遵循固定点量纲、舍入、符号、除零和溢出规则；失败不扣预算、不写覆盖或派生结论。
5. 设备拒绝、断连、超时和格式错误生成带逻辑时间且顺序、对象、次数确定的待重试调用；进程重启后可继续重试，系统从不制造观察值。
6. 农药事件按施用分区、漂移邻接和再入间隔原子冻结；冻结回执不推进覆盖，迟到事件保留证据并提升恢复代次，完成蜂群撤回和残留核验前不得恢复。
7. 覆盖不足、回巢异常或连续失重只能产生一个稳定排序的补授粉范围并重新占用相关分区时点；旧代次回执不能覆盖当前证据。
8. 覆盖闭合、预算守恒、冻结解除、补授粉结束及两名不同合格人员独立复核后，仅一个并发请求生成唯一完成凭据；完成、撤离、取消只能形成一个持久化终局，终态不可变。

## 确定性测试场景

1. 在虚构的三温室多分区邻接图上锁定完整周期，断言快照字段、覆盖格数量、占用窗口、预算初值和证据顺序；至少置于 cycle_lock_test.go。
2. 并发提交同蜂群重叠窗口、同分区不相容蜂群和多个预算扣减，使用同步屏障断言唯一胜者、余额守恒及无部分占用；置于 occupancy_concurrency_test.go。
3. 使用相同及冲突 operation_id 重试锁定、轮换和观察，精确断言响应复用、OPERATION_CONTENT_CONFLICT 和稳定原因排序。
4. 提交缺失观察点、重复冲突观察、访花分项不守恒、咬痕加未授粉越界及错误代次，断言覆盖格与预算均不变化；置于 coverage_ledger_test.go。
5. 覆盖固定点正常舍入、负值、除零、乘法溢出、重量连续下降、回巢率和咬痕率边界，精确断言整数结果与错误码；置于 fixedpoint_metrics_test.go。
6. 用可控故障脚本模拟环境探头、蜂箱秤和计数适配器的拒绝、断连、超时、格式错误及随后成功，断言逻辑退避、重试键、次数和重启恢复；置于 device_retry_test.go。
7. 投递正常和迟到农药事件，验证施用分区及漂移邻区冻结传播、冻结回执隔离、旧恢复凭据失效、新恢复代次及撤回和残留核验门槛；置于 pesticide_recovery_test.go。
8. 构造缺格、低回巢率和连续失重的重叠异常，断言只生成一个合并且排序稳定的补授粉范围、重新占用冲突以及旧代次回执隔离；置于 supplement_test.go。
9. 并发提交两名复核、完成、撤离和取消，使用同步屏障精确断言单一终局、唯一凭据和终态不变；置于 terminal_race_test.go。
10. 在事务故障注入点分别中断占用建立、预算扣减、覆盖写入、冻结传播和终局提交，重启服务后断言仅出现完整提交前或完整提交后状态；置于 persistence_recovery_test.go。
11. 公共测试总数不少于 12 个独立用例并分布于至少 4 个测试文件；统一使用固定标识、整数化读数、逻辑时钟、内存或临时文件数据库、确定故障脚本和同步屏障，禁止依赖真实时间及随机调度。
12. 为后续缺陷任务预留至少 10 个相互独立、可由上述断言捕获的行为边界，例如区间端点、幂等摘要、预算丢失更新、覆盖重复、固定点溢出、重试次数、邻接传播、恢复代次、补授粉合并和终局竞争；所有缺陷分支从同一初始 main 独立建立。

## 组件追踪关系

1. 规则目录：支撑验收 1、2、6、8；建议拆分 greenhouse.go、zone.go、colony.go、certificate.go、snapshot.go 等生产文件。
2. 周期聚合：支撑验收 1、2、3、7、8；建议拆分 cycle.go、state.go、commands.go、idempotency.go、evidence.go、terminal.go。
3. 占用管理器：支撑验收 2、7；建议拆分 interval.go、conflict_graph.go、occupancy.go、budget.go、rotation.go。
4. 覆盖与指标账簿：支撑验收 3、4、5、7；建议拆分 coverage.go、observation.go、fixedpoint.go、metrics.go、device.go、retry.go、supplement.go。
5. 冻结恢复及结案仲裁器：支撑验收 6、8；建议拆分 pesticide.go、freeze.go、recovery.go、review.go、arbitration.go。
6. HTTP API 与持久化适配层：贯穿全部验收；建议包含 router.go、handlers_cycle.go、handlers_observation.go、handlers_safety.go、errors.go、store.go、migrations.go、recovery_worker.go 和 cmd/server/main.go。最终组织至少 4 个有业务意义的包、至少 20 个生产 Go 文件；必须包含 go.mod、数据库迁移、可编译启动入口及基于 buildx 的 linux/amd64 与 linux/arm64 Docker 构建配置。

## 独特性

本计划专注设施番茄花期熊蜂的分区轮换授粉，将区间占用、可消耗巡飞预算、花序观察覆盖格、农药漂移冻结、蜂王与恢复代次、补授粉合并和双人终局复核统一为一个可恢复事务闭环；标题与既有农业食品联检项目不同，也不引入品牌或无关子系统。
